/**
 * Model for the users that are selectable in the transfer dialog.
 */
export interface TransferSelectable {
  /**
   * Name of the user.
   */
  name: string;
  /**
   * Unique ID of the user.
   */
  id: number;
}
